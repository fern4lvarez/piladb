// piladb is a RESTful database engine based on the stack data structure.
// Create an ilimited number of stacks, which can contain any kind of supported data types.
package main

import "fmt"

func main() {
	fmt.Println("file generate to make project root go-getable.")
}
